import 'package:dynamic_flutter/data/model/dynamic_json.dart';
import 'package:flutter/material.dart';

class WidgetParser extends StatelessWidget {
  const WidgetParser({Key? key, required this.dynamicJson}) : super(key: key);
  final DynamicJson dynamicJson;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
